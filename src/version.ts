/** Package identity, shared by the MCP handshake, the CLI and generated clients' X-Swfte-Client header. */
export const PACKAGE_NAME = '@swfte/mcp-server';
// Kept equal to package.json "version" by test/bake.test.ts.
export const PACKAGE_VERSION = '0.2.0';
